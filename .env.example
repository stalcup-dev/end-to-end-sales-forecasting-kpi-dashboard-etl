# PostgreSQL Database Configuration
# Copy this file to .env and fill in your credentials

# Option 1: Use full database URI
DB_URI=postgresql://username:password@localhost:5432/vitamarkets

# Option 2: Use individual parameters (DB_URI takes precedence if both are set)
PG_USER=postgres
PG_PASS=yourpassword
PG_HOST=localhost
PG_PORT=5432
PG_DB=vitamarkets
